version: 2

models:
  - name: fact_taxi_trips
    description: "The primary fact table for taxi trip analysis."
    columns:
      - name: trip_id
        tests:
          - unique
          - not_null
      - name: total_revenue
        tests:
          - not_null